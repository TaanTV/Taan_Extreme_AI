icon = GFX_district_specialization_unity
base_buildtime = @zone_buildtime
potential = { # planet scope
	hidden_trigger = { exists = owner }
	owner = {
		is_wilderness_empire = no
		is_spiritualist = no
		NOT = {
			has_valid_civic = civic_machine_servitor
		}
	}
	OR = {
		owner = {
			is_ai = no
		}
		AND = {
			has_any_research_zone = no
			has_any_industrial_zone = no
		}
		exists = owner_or_space_owner
		owner_or_space_owner = { AND = { meets_basic_resource_thresholds = yes OR = { AND = { years_passed < 120 num_ascension_perks < 3 } AND = { years_passed < 180 num_ascension_perks < 4 } } } }
	}
}

unlock = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			OR = {
				is_wilderness_empire = no
				NOT = { has_valid_civic = civic_machine_servitor }
			}
		}
	}
}

resources = {
	category = planet_zones
	cost = {
		minerals = @zone_cost
	}
}

included_building_sets = {
	unity
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_unity_jobs_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = 3
}

ai_resource_production = {
	unity = 15

	trigger = {
		has_any_unity_zone = yes
	}
}

ai_resource_production = {
	unity = 16

	trigger = {
		exists = owner_or_space_owner
		owner_or_space_owner = { AND = { meets_basic_resource_thresholds = yes OR = { AND = { years_passed < 120 num_ascension_perks < 3 } AND = { years_passed < 180 num_ascension_perks < 4 } } } }
		NOT = { has_planet_flag = stellarai_invalid_for_unity }
	}
}

ai_priority = @ai_zone_priority_research_unity
