icon = GFX_district_specialization_foundry
base_buildtime = @zone_buildtime

potential = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			is_wilderness_empire = no
		}
		inline_script = {
			script = stellarai/invalid_colony_type_potential
			PLANET_FLAG = stellarai_invalid_for_foundry_to_build
		}
	}
}

unlock = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			is_wilderness_empire = no
		}
		inline_script = {
			script = stellarai/invalid_colony_type_potential
			PLANET_FLAG = stellarai_invalid_for_foundry_to_build
		}
	}
}

resources = {
	category = planet_zones
	cost = {
		minerals = @zone_cost
	}
}

included_building_sets = {
	foundry
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_foundry_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = 3
}

ai_resource_production = {
	alloys = 20

	trigger = {
		has_any_foundry_zone = yes
		#NOT = { has_planet_flag = stellarai_invalid_for_foundry_to_build }
	}
}

ai_priority = @ai_zone_priority_industry
ai_weight_coefficient = @ai_zone_priority_industry_coefficient
